-- main.gr — project entry point.
--
-- `print` is a statement, not a function, so there is nothing to
-- import to say hello. Build and run with:
--
--   gero build
--   gero run out/debug/<name>.gx

def main()
  print "Hello, gero!"
end
